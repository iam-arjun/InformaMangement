import { Filterpipe6Pipe } from './filterpipe6.pipe';

describe('Filterpipe6Pipe', () => {
  it('create an instance', () => {
    const pipe = new Filterpipe6Pipe();
    expect(pipe).toBeTruthy();
  });
});
