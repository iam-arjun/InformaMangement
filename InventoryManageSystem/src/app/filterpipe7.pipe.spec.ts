import { Filterpipe7Pipe } from './filterpipe7.pipe';

describe('Filterpipe7Pipe', () => {
  it('create an instance', () => {
    const pipe = new Filterpipe7Pipe();
    expect(pipe).toBeTruthy();
  });
});
